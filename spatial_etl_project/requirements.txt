# Core dependencies (required)
dlt[duckdb]>=0.4.0
shapely>=2.0.0
pyproj>=3.5.0

# Apache Sedona + PySpark - must use compatible versions
# PySpark 3.5.x works with Sedona 1.6.0
# PySpark 4.x has breaking changes and is NOT compatible with Sedona 1.6.0
pyspark>=3.5.0,<4.0.0
numpy>=1.24.0,<2.0.0  # Pin to avoid 2.x which requires compilation
apache-sedona==1.6.0

# Optional: GDAL/OGR (for Shapefile, CAD, Raster formats)
# Uncomment after installing system GDAL (brew install gdal)
# gdal>=3.6.0

# Optional: PostGIS support
# psycopg2-binary>=2.9.0

# Optional: Advanced raster processing
# rasterio>=1.3.0
# fiona>=1.9.0

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0

# Development
black>=23.0.0
pylint>=2.16.0
mypy>=1.0.0

# Jupyter notebook support (optional)
# jupyter>=1.0.0
# ipykernel>=6.20.0
