[[repos]]
repo = "local"

[[repos.hooks]]
id = "pre-push-gate"
name = "dlt pre-push gate"
language = "system"
entry = "uv run python -m tools.prek"
pass_filenames = false
always_run = true
stages = ["pre-push"]
