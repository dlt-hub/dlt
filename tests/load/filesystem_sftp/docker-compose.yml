version: '3.8'

services:

  sftpserver:
    build:
      context: bootstrap
      dockerfile: Dockerfile
    image: sftpserver:latest
    networks:
      - sftpserver
    ports:
      - "2222:22"
    volumes:
      - ../../common/storages/samples:/home/foo/sftp/data/samples 

networks:
  sftpserver:
    name: sftpserver
