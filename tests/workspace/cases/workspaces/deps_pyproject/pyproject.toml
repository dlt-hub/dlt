[project]
name = "test-workspace-deps"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "tomlkit>=0.12",
    "packaging>=23.0",
]

[dependency-groups]
dev = ["pytest>=7.0"]
gpu = ["numpy>=1.24"]
