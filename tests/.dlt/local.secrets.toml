# This file contains the secrets for running tests against locally instantiated destinations
# and is checked into the repository so developers may copy these configurations into their
# `secrets.toml`. Also this file is used by CI.
#
# DO NOT ADD ANY REAL SECRETS HERE!
#

